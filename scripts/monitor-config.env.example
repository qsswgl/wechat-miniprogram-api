# WeChat API 监控脚本邮件告警配置示例
# 将此文件复制为 monitor-config.env 并填写实际值

# 启用邮件告警（true/false）
ALERT_EMAIL_ENABLED=false

# SMTP 服务器配置
SMTP_SERVER=smtp.139.com
SMTP_PORT=25

# SMTP 认证信息
SMTP_USER=your-email@139.com
SMTP_PASS=your-auth-code

# 告警接收邮箱（可多个，用逗号分隔）
ALERT_EMAIL_TO=admin@example.com

# 使用方法：
# 1. 复制此文件：cp monitor-config.env.example monitor-config.env
# 2. 编辑 monitor-config.env 填写实际配置
# 3. 在 crontab 中引用：
#    */5 * * * * source /opt/wechat-api/scripts/monitor-config.env && /bin/bash /opt/wechat-api/scripts/monitor-wechat-api.sh
